- name: Install k3s agent
  ansible.builtin.shell: |
    curl -sfL https://get.k3s.io | INSTALL_K3S_CHANNEL=v1.31.1+k3s1 sh -
  args:
    creates: /usr/local/bin/k3s
  environment:
    INSTALL_K3S_EXEC: agent
    K3S_TOKEN: "{{ k3s_token }}"
    K3S_URL: "{{ k3s_url }}"
